# 🔗 页面导航集成指南

## 📌 如何在首页添加到 Sowaka 页面的链接

### 方法一：在首页导航菜单中添加（推荐）

打开 `src/views/HomePage.vue`，找到导航菜单部分，添加新的导航项：

```vue
<!-- 在导航菜单中添加 -->
<nav class="nav-menu">
  <div class="nav-sentence">
    <div class="nav-part">
      <router-link to="/blue" class="nav-link">昔の詩</router-link>を辿り、
    </div>
    <div class="nav-part">
      <router-link to="/sowaka" class="nav-link">そわか</router-link>の宿へ訪れ、
    </div>
    <div class="nav-part">
      <a href="#" class="nav-link">出会った人々</a>と共に、
    </div>
    <div class="nav-part">
      <a href="#" class="nav-link">果てしない道</a>を歩む。
    </div>
  </div>
</nav>
```

### 方法二：添加独立按钮

在页脚文字区域添加独立按钮：

```vue
<div class="footer-text">
  <span>そして</span>
  <span>また</span>
  <span>春が来て</span>
  <span>秋が来て</span>
  <span>四季が巡ります。</span>
  
  <!-- 添加到 Sowaka 的按钮 -->
  <div style="margin-top: 40px;">
    <router-link to="/sowaka" class="sowaka-button">
      訪れる　そわか
    </router-link>
  </div>
</div>
```

然后添加对应的样式：

```css
.sowaka-button {
  display: inline-block;
  padding: 15px 40px;
  color: #fff;
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.3);
  text-decoration: none;
  letter-spacing: 3px;
  font-size: 14px;
  transition: all 0.3s;
  backdrop-filter: blur(10px);
}

.sowaka-button:hover {
  background: rgba(255, 255, 255, 0.2);
  border-color: rgba(255, 255, 255, 0.5);
  transform: translateY(-2px);
}
```

### 方法三：从 Sowaka 页面返回首页

在 `src/views/SowakaPage.vue` 中，导航栏的 logo 添加点击返回首页的功能：

找到导航栏的 logo 部分，修改为：

```vue
<div class="logo">
  <router-link to="/" style="color: inherit; text-decoration: none;">
    そわか
  </router-link>
</div>
```

或者在导航链接中添加"ホーム"链接：

```vue
<ul class="nav-links">
  <li><router-link to="/">ホーム</router-link></li>
  <li><a href="#story" @click="scrollToSection">物語</a></li>
  <li><a href="#concept" @click="scrollToSection">コンセプト</a></li>
  <li><a href="#rooms" @click="scrollToSection">客室</a></li>
  <li><a href="#news" @click="scrollToSection">お知らせ</a></li>
  <li><a href="#access" @click="scrollToSection">アクセス</a></li>
</ul>
```

## 🎨 多页面导航建议

### 创建统一的导航组件

为了更好地管理多个页面之间的导航，建议创建一个独立的导航组件：

1. **创建导航组件**

创建文件 `src/components/MainNavigation.vue`:

```vue
<template>
  <nav class="main-navigation">
    <router-link to="/" class="nav-item">首页</router-link>
    <router-link to="/blue" class="nav-item">蓝色世界</router-link>
    <router-link to="/sowaka" class="nav-item">そわか</router-link>
  </nav>
</template>

<script setup>
</script>

<style scoped>
.main-navigation {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  display: flex;
  gap: 20px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  padding: 15px 25px;
  border-radius: 30px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.nav-item {
  color: #333;
  text-decoration: none;
  font-size: 14px;
  letter-spacing: 2px;
  transition: opacity 0.3s;
}

.nav-item:hover {
  opacity: 0.6;
}

.nav-item.router-link-active {
  font-weight: 600;
  opacity: 1;
}
</style>
```

2. **在需要的页面中引入**

```vue
<script setup>
import MainNavigation from '@/components/MainNavigation.vue'
</script>

<template>
  <div>
    <MainNavigation />
    <!-- 页面内容 -->
  </div>
</template>
```

## 📋 页面跳转方式总结

### 1. 声明式导航（模板中）

```vue
<!-- 基础用法 -->
<router-link to="/sowaka">前往 Sowaka</router-link>

<!-- 命名路由 -->
<router-link :to="{ name: 'sowaka' }">前往 Sowaka</router-link>

<!-- 带查询参数 -->
<router-link :to="{ path: '/sowaka', query: { room: 'garden' } }">
  查看庭園の間
</router-link>
```

### 2. 编程式导航（脚本中）

```javascript
import { useRouter } from 'vue-router'

const router = useRouter()

// 基础跳转
router.push('/sowaka')

// 命名路由跳转
router.push({ name: 'sowaka' })

// 带参数跳转
router.push({ 
  path: '/sowaka', 
  query: { room: 'moon' } 
})

// 替换当前历史记录（不可返回）
router.replace('/sowaka')

// 返回上一页
router.back()

// 前进到下一页
router.forward()
```

### 3. 带动画的页面跳转

```vue
<script setup>
import { useRouter } from 'vue-router'

const router = useRouter()

const navigateWithAnimation = async () => {
  // 添加淡出效果
  document.body.style.opacity = '0'
  document.body.style.transition = 'opacity 0.5s'
  
  // 等待动画完成
  await new Promise(resolve => setTimeout(resolve, 500))
  
  // 跳转页面
  router.push('/sowaka')
  
  // 页面加载后淡入
  document.body.style.opacity = '1'
}
</script>

<template>
  <button @click="navigateWithAnimation">
    优雅跳转到 Sowaka
  </button>
</template>
```

## 🎯 实际应用示例

### 示例 1: 在首页添加图片卡片链接

```vue
<template>
  <div class="page-cards">
    <router-link to="/blue" class="page-card">
      <div class="card-image blue-theme"></div>
      <h3>蓝色世界</h3>
      <p>探索夏日雨天的诗意</p>
    </router-link>
    
    <router-link to="/sowaka" class="page-card">
      <div class="card-image sowaka-theme"></div>
      <h3>そわか</h3>
      <p>体验京都传统之美</p>
    </router-link>
  </div>
</template>

<style scoped>
.page-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 30px;
  padding: 50px;
  max-width: 1200px;
  margin: 0 auto;
}

.page-card {
  text-decoration: none;
  color: inherit;
  background: #fff;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  transition: transform 0.3s, box-shadow 0.3s;
}

.page-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}

.card-image {
  width: 100%;
  height: 200px;
  background-size: cover;
  background-position: center;
}

.blue-theme {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.sowaka-theme {
  background: linear-gradient(135deg, #d4c5b0 0%, #c4b5a0 100%);
}

.page-card h3 {
  padding: 20px 20px 10px;
  font-size: 22px;
  letter-spacing: 2px;
}

.page-card p {
  padding: 0 20px 20px;
  color: #666;
  font-size: 14px;
}
</style>
```

### 示例 2: 面包屑导航

```vue
<template>
  <div class="breadcrumb">
    <router-link to="/">ホーム</router-link>
    <span class="separator">/</span>
    <router-link to="/sowaka">そわか</router-link>
    <span class="separator" v-if="currentRoom">/</span>
    <span v-if="currentRoom">{{ currentRoom }}</span>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useRoute } from 'vue-router'

const route = useRoute()
const currentRoom = ref(route.query.room || '')
</script>

<style scoped>
.breadcrumb {
  padding: 20px 50px;
  font-size: 13px;
  color: #666;
}

.breadcrumb a {
  color: #333;
  text-decoration: none;
  transition: color 0.3s;
}

.breadcrumb a:hover {
  color: #c9a96e;
}

.separator {
  margin: 0 10px;
  color: #999;
}
</style>
```

## 🎨 导航样式建议

### 浮动导航按钮

```vue
<style>
.floating-nav {
  position: fixed;
  bottom: 30px;
  right: 30px;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.floating-nav-item {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  color: #333;
  transition: all 0.3s;
}

.floating-nav-item:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 20px rgba(0,0,0,0.15);
}
</style>
```

## 📝 注意事项

1. **使用 `router-link` 而不是 `<a>`**
   - 保持 SPA 体验
   - 避免页面完全刷新
   - 支持路由高亮

2. **适当使用编程式导航**
   - 需要条件判断时
   - 需要携带复杂参数时
   - 需要在异步操作后跳转时

3. **保持导航一致性**
   - 所有页面的返回首页方式保持一致
   - 导航样式统一
   - 移动端体验优化

4. **考虑用户体验**
   - 添加页面加载动画
   - 提供清晰的返回路径
   - 移动端优化触摸区域

---

**通过合理的导航设计，让用户在不同页面间流畅切换！** 🚀


