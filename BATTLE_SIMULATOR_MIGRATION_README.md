# 战斗模拟器迁移说明

## 📋 概述

本文档说明了将战斗模拟器从纯前端HTML/JS架构迁移到Vue + 后端API架构的完整过程。

## 🎯 迁移目标

- ✅ 将 `new2.html` 和 `new2.js` 迁移到 Vue 单文件组件
- ✅ 将战斗逻辑、AI调用等核心功能移至后端
- ✅ 前端只负责UI渲染和用户交互
- ✅ 创建数据库存储角色数据
- ✅ 保持原有样式不变

## 📁 文件结构

### 前端文件 (web-project/)

```
web-project/
├── src/
│   ├── views/
│   │   └── BattleSimulatorPage.vue    # 主组件（迁移自 new2.html）
│   └── router/
│       └── index.js                    # 添加了路由配置
├── package.json                        # 添加了 marked 依赖
```

### 后端文件 (backend/)

```
backend/
├── src/
│   ├── controllers/
│   │   └── battleSimulatorController.js    # 战斗逻辑控制器
│   ├── routes/
│   │   └── battleSimulatorRoutes.js        # API路由
│   ├── schemas/
│   │   ├── battleSimulatorSchema.js        # 战斗会话Schema
│   │   └── battleCharacterSchema.js        # 角色数据Schema
│   └── scripts/
│       └── initBattleCharacters.js         # 角色数据初始化脚本
├── config.js                               # 添加了AI API配置
├── env.example                             # 添加了AI API环境变量示例
├── init-battle-characters.bat              # 角色初始化批处理
├── package.json                            # 添加了 node-fetch 依赖
└── BATTLE_CHARACTERS_GUIDE.md              # 角色数据库使用指南
```

### 角色数据文件 (根目录)

```
├── yan_shuang_ying_boss.json   # 燕双鹰Boss数据
└── yongzhe_ma.json             # 勇者·马云飞数据
```

## 🚀 快速开始

### 1. 安装依赖

#### 前端依赖
```bash
cd web-project
npm install
# 新增依赖: marked@^11.1.0
```

#### 后端依赖
```bash
cd backend
npm install
# 新增依赖: node-fetch@^2.7.0
```

### 2. 配置环境变量

复制并编辑环境变量文件：

```bash
cd backend
copy env.example .env
```

编辑 `.env` 文件，添加AI API配置：

```env
# AI API 配置（用于战斗模拟器）
AI_API_KEY=your_api_key_here
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL_NAME=gpt-4
```

### 3. 初始化角色数据

```bash
cd backend
npm run init-battle-characters
```

或者直接运行批处理文件：

```bash
cd backend
init-battle-characters.bat
```

### 4. 启动服务

#### 启动后端
```bash
cd backend
npm run dev
```

后端将运行在 `http://localhost:3000`

#### 启动前端
```bash
cd web-project
npm run dev
```

前端将运行在 `http://localhost:5173`

### 5. 访问页面

打开浏览器访问：`http://localhost:5173/battle-simulator`

## 🔄 架构变化

### 原架构 (new2.html + new2.js)

```
┌─────────────────────────────────────┐
│         浏览器 (前端)                │
│                                     │
│  ┌──────────────────────────────┐  │
│  │  HTML (new2.html)            │  │
│  │  - UI渲染                    │  │
│  │  - 用户交互                  │  │
│  │  - API配置输入               │  │
│  └──────────────────────────────┘  │
│                                     │
│  ┌──────────────────────────────┐  │
│  │  JavaScript (new2.js)        │  │
│  │  - 角色数据 (硬编码)        │  │
│  │  - 战斗逻辑                  │  │
│  │  - AI API调用                │  │
│  │  - 伤害计算                  │  │
│  │  - 状态管理                  │  │
│  └──────────────────────────────┘  │
│            │                        │
└────────────┼────────────────────────┘
             │
             ▼
      ┌──────────────┐
      │   AI API     │
      │ (OpenAI等)   │
      └──────────────┘
```

### 新架构 (Vue + Backend API)

```
┌──────────────────────────────────────────┐
│            浏览器 (前端)                  │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Vue Component                     │ │
│  │  (BattleSimulatorPage.vue)        │ │
│  │                                    │ │
│  │  - UI渲染                         │ │
│  │  - 用户交互                       │ │
│  │  - 显示战斗结果                   │ │
│  │  - 本地状态管理 (health等)       │ │
│  └────────────────────────────────────┘ │
│              │                           │
└──────────────┼───────────────────────────┘
               │ HTTP API
               ▼
┌──────────────────────────────────────────┐
│          后端服务器 (Node.js)             │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Routes                            │ │
│  │  (battleSimulatorRoutes.js)       │ │
│  └────────────────────────────────────┘ │
│              │                           │
│  ┌────────────────────────────────────┐ │
│  │  Controller                        │ │
│  │  (battleSimulatorController.js)   │ │
│  │                                    │ │
│  │  - 战斗逻辑                       │ │
│  │  - 伤害计算                       │ │
│  │  - AI Prompt构建                  │ │
│  │  - 结果处理                       │ │
│  └────────────────────────────────────┘ │
│              │                           │
│  ┌────────────────────────────────────┐ │
│  │  Database (MongoDB)                │ │
│  │                                    │ │
│  │  - 角色数据                       │ │
│  │  - 战斗记录 (可选)                │ │
│  └────────────────────────────────────┘ │
│              │                           │
└──────────────┼───────────────────────────┘
               │
               ▼
        ┌──────────────┐
        │   AI API     │
        │ (OpenAI等)   │
        └──────────────┘
```

## 🔌 API 端点

### 1. 初始化战斗
```http
GET /api/battle-simulator/init?enemyId=HimuroRinne
```

**响应：**
```json
{
  "success": true,
  "data": {
    "enemyProfile": { "name": "...", "moves": [...] },
    "userProfile": { "name": "...", "moves": [...] },
    "userMoves": [...]
  }
}
```

### 2. 执行战斗回合
```http
POST /api/battle-simulator/execute-turn
Content-Type: application/json

{
  "enemyId": "HimuroRinne",
  "selectedMoveId": "vega_arrow",
  "battleHistory": [...],
  "activeMoves": [...],
  "disabledMoves": {...},
  "userHealth": 5,
  "aiHealth": 5
}
```

**响应：**
```json
{
  "success": true,
  "data": {
    "turnData": {
      "userMoveName": "...",
      "ai": "...",
      "novelText": "...",
      "damageToAI": 2,
      "damageToUser": 0
    },
    "userHealth": 5,
    "aiHealth": 3,
    "isBattleEnd": false,
    "newMoves": [...],
    "disabledMoves": {...}
  }
}
```

### 3. 获取角色列表
```http
GET /api/battle-simulator/characters?type=enemy
```

**响应：**
```json
{
  "success": true,
  "data": [
    {
      "characterId": "YanShuangYingEagle",
      "name": "燕双鹰eagle·栩",
      "desc": "传说中的因果律恶搞Boss...",
      "difficulty": "meme",
      "tags": ["神剧", "因果系"]
    }
  ]
}
```

### 4. 重置战斗
```http
POST /api/battle-simulator/reset
Content-Type: application/json

{
  "enemyId": "HimuroRinne"
}
```

### 5. 加载自定义角色
```http
POST /api/battle-simulator/load-enemy
Content-Type: application/json

{
  "id": "CustomEnemy",
  "name": "自定义角色",
  "desc": "...",
  "moves": [...]
}
```

## 📊 数据流

### 战斗回合执行流程

```
1. 用户选择招式
   ↓
2. 前端发送 POST /execute-turn
   ↓
3. 后端接收请求
   ├─ 从数据库加载敌方角色数据
   ├─ 验证招式是否可用
   ├─ 构建AI Prompt
   ├─ 调用AI API生成战斗文本
   ├─ 计算伤害
   ├─ 处理动态招式
   ├─ 处理招式禁用
   └─ 检查战斗是否结束
   ↓
4. 返回结果给前端
   ↓
5. 前端更新UI
   ├─ 显示战斗文本
   ├─ 更新健康值
   ├─ 更新招式列表
   ├─ 添加系统日志
   └─ 检查战斗结束
```

## 🎨 样式保持

迁移过程中保持了原有的所有样式：

- ✅ Moonlight Grail 主题风格
- ✅ 玻璃态卡片效果
- ✅ 招式卡片悬浮动画
- ✅ 健康值心形显示
- ✅ 战斗日志滚动效果
- ✅ 所有颜色、字体、间距

## 🆕 新增功能

### 1. 数据库角色管理
- 角色数据存储在 MongoDB
- 支持动态加载角色
- 支持自定义角色导入

### 2. 角色列表API
- 可查询所有可用角色
- 支持按类型筛选（敌方/玩家）
- 包含难度、标签等元数据

### 3. 安全性提升
- API Key 不再暴露给前端
- 所有敏感操作在后端执行
- 防止客户端篡改战斗结果

## 📝 配置文件说明

### backend/config.js
```javascript
ai: {
  apiKey: process.env.AI_API_KEY || '',
  baseUrl: process.env.AI_BASE_URL || 'https://api.openai.com/v1',
  modelName: process.env.AI_MODEL_NAME || 'gpt-4'
}
```

### web-project/package.json
新增依赖：
```json
{
  "dependencies": {
    "marked": "^11.1.0"  // Markdown渲染
  }
}
```

### backend/package.json
新增依赖：
```json
{
  "dependencies": {
    "node-fetch": "^2.7.0"  // HTTP请求
  },
  "scripts": {
    "init-battle-characters": "node src/scripts/initBattleCharacters.js"
  }
}
```

## 🔧 故障排除

### 问题1: marked 依赖未找到
```bash
# 解决方案
cd web-project
npm install marked
```

### 问题2: node-fetch 依赖未找到
```bash
# 解决方案
cd backend
npm install node-fetch@2
```

### 问题3: MongoDB 连接失败
```bash
# 检查 MongoDB 是否运行
net start MongoDB

# 或启动 MongoDB Compass
```

### 问题4: AI API 调用失败
```
检查：
1. .env 中 AI_API_KEY 是否配置
2. AI_BASE_URL 是否正确
3. 网络连接是否正常
```

### 问题5: 前端无法连接后端
```
检查：
1. 后端是否在 localhost:3000 运行
2. 前端的API地址是否正确
3. CORS配置是否正确
```

## 📚 相关文档

- [角色数据库使用指南](./backend/BATTLE_CHARACTERS_GUIDE.md)
- [后端API文档](./backend/README.md)
- [MongoDB 快速启动](./backend/MongoDB快速启动指南.md)

## ✅ 迁移清单

- [x] 创建 Vue 组件 `BattleSimulatorPage.vue`
- [x] 创建后端控制器 `battleSimulatorController.js`
- [x] 创建后端路由 `battleSimulatorRoutes.js`
- [x] 创建角色数据 Schema `battleCharacterSchema.js`
- [x] 创建战斗会话 Schema `battleSimulatorSchema.js`
- [x] 创建初始化脚本 `initBattleCharacters.js`
- [x] 添加前端路由配置
- [x] 添加API配置到 `config.js`
- [x] 添加环境变量示例
- [x] 安装必要依赖
- [x] 编写文档

## 🎯 下一步

1. **测试功能**
   - 测试战斗流程
   - 测试角色加载
   - 测试存档功能

2. **优化性能**
   - 添加请求缓存
   - 优化AI调用
   - 减少不必要的状态更新

3. **增强功能**
   - 添加战斗回放
   - 添加排行榜
   - 添加成就系统

## 📞 联系与支持

如有问题，请参考：
- 文档：`BATTLE_CHARACTERS_GUIDE.md`
- 示例：`yan_shuang_ying_boss.json`, `yongzhe_ma.json`
- 原始代码：`new2.html`, `new2.js`

---

**迁移完成日期**: 2025-01-06
**版本**: 1.0.0




























